# Sentry configuration for Android builds
# Upload is disabled during EAS builds
# Configure SENTRY_ORG and SENTRY_PROJECT via EAS secrets if you want to enable upload

defaults.project=
defaults.org=

